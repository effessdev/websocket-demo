# WebSocket demo

A very simple implementation of the WebSocket protocol.

## Installation

```bash
npm init -y
npm install ws
```

## Running

```
node server.js
```

Then open `client.html` in multiple browser tabs.
